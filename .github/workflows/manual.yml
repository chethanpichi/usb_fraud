name: copy files
on: [push]
jobs:

  build:
    name: Build
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@main
    - name: copy file
      uses: canastro/copy-file-action@main
      with:
        source: "dag/dag_test.py"
        target: "dag_test_1.py"
