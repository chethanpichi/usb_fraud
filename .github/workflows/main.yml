name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: push
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: run the code
      - name: echo this is my first step.
